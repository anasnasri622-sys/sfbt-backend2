services:
  - type: web
    name: sfbt-ai-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: GEMINI_API_KEY
        sync: false  # à saisir dans le dashboard Render
